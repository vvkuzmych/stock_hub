version: '3.8'

services:
  mailhog:
    image: mailhog/mailhog:latest
    container_name: stock_hub_mailhog
    ports:
      - "1025:1025"   # SMTP server
      - "8025:8025"   # Web UI
    networks:
      - stock_hub_network

networks:
  stock_hub_network:
    driver: bridge
